# grants-config-feature-controls

Feature control definitions that provide a means to configure the platform via hot-switchable configuration values.

## Language

**Feature control**
A control that determines a single value that can be set per environment, and changed at run time via a UI for proliferation across the platform
_Avoid_: Flag, Toggle, Switch
